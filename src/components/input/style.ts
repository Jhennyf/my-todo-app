import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    width: "100%",
    height: 56,
    borderRadius: 8,
    borderWidth: 1,
    borderColor: "#000",
    padding: 16,
    fontSize: 16,
    color: "#000",
    backgroundColor: "#fff",
  },
});
